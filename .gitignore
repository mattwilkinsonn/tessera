# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo
/tess

# Nix flake build output
result
result-*

# devenv / direnv — devenv.lock is intentionally NOT committed (matches main;
# the shell is a launcher, its lock is a local artifact that churns per-machine).
.devenv/
.direnv/
devenv.lock
devenv.local.nix
.envrc.local

# Logs
*.log

# macOS
.DS_Store
